route:
  receiver: taskpulse-webhook
  group_wait: 2s
  group_interval: 10s
  repeat_interval: 1h
receivers:
  - name: taskpulse-webhook
    webhook_configs:
      - url: http://alert-receiver:9094/alerts
        send_resolved: true
